apiVersion: v1
kind: Service
metadata:
  name: datastore-frontend-lb
  namespace: {{ .Values.StudentStoreFrontend.studentSvc.namespace }}
spec:
  type: LoadBalancer
  selector:
    type: frontend
  ports:
    - port: 80
      targetPort: 8080
